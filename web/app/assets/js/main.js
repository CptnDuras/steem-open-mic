/*
 This is the main Vue component file for steem-open-mic

 This should be browser-ready JS, not ES6/Next transpiled JS
 The philosophy there is to KISS. Flask can serve up JS and webpack is a PITA
*/
var app = new Vue({
    el: '#app'
});